replicaCount: 3

image:
  repository: prasidsash/employeeprofileapp
  tag: 0
  pullPolicy: Always

service:
  nodePort: 30503

ingress:
  enabled: true

environment: prod

autoscaling:
  enabled: true
  minReplicas: 4
  maxReplicas: 10
  cpu: 60