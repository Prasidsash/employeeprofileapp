replicaCount: 3

image:
  repository: prasidsash/employeeprofileapp
  tag: 0
  pullPolicy: Always

service:
  nodePort: 30500

ingress:
  enabled: true

environment: dev

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  cpu: 70