serviceAccount:
  name: employee-sa

service:
  type: NodePort
  port: 80
  targetPort: 8080
  nodePort: null         # 👈 add this

ingress:
  enabled: false
  className: traefik
  hosts: []

secret:
  name: employee-db-secret